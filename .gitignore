# Dipendenze (si rigenerano con npm install / npm ci)
node_modules/

# Output di build (generato da Vite, pubblicato dalla GitHub Action)
dist/

# Documentazione privata della skill (non va in repo)
docs/SKILL.md

# Script locale di controllo pre-commit/deploy (non parte del bundle)
check-all.bat

# File di sistema/editor comuni
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
